using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefenseCharacterScript : MonoBehaviour
{
    //보호할 캐릭터 프리팹 설정
    //일정 거리 이상 떨어지지 않도록 한다
    public GameObject m_defensePrefab;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
